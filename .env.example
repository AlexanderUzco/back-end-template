NODE_ENV='example'
PORT=3000

# Database
BASE_DB_URI=mongodb://mongo:27017/base

# JWT
JWT_SECRET=change_me_super_secret
JWT_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=1d

# Refresh tokens absolute max lifetime (sliding window cap)
REFRESH_MAX_TTL=7d